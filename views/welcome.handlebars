<h2 class="page-header" style="color:#F5F5F5"> Welcome To Portfolio Tracker</h2>
{{#if errors}}
      {{#each errors}}
        <div class="alert alert-danger">{{msg}}</div>
      {{/each}}
{{/if}}
<style>
   body{
     background-image: url("images.jpeg");
     background-color:#2c3e50;
   }
</style>

<div class="btn-group" role="group" aria-label="...">
  <form method="get" action="/students/login">
  <button type="submit" class="btn btn-default">
  <span class="glyphicon glyphicon-ok-sign" aria-hidden="true"></span>Having an account?</button></form>
  <form method="get" action="/guest">
  <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-star" aria-hidden="true"></span>Guest</button>
</form>
<form method="get" action="/guestAll">
<button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-star" aria-hidden="true"></span>Guest(See All Projects)</button>
</form>
</div>
