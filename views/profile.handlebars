<h2 class="page-header" style="color:#F5F5F5"> Welcome To Your Profile</h2>
{{#if errors}}
      {{#each errors}}
        <div class="alert alert-danger">{{msg}}</div>
      {{/each}}
{{/if}}
<style>
   body{
     background-image: url("images.jpeg");
     background-color:#2c3e50;
   }
</style>

<body>

  <div class="form-group" >
    <label>Username: {{user.username}}</label>
    <input type="text" style="display:none;" class="form-control" placeholder="Username" name="username">
  </div>
  <div class="form-group" >
    <label>Email: {{user.email}}</label>
    <input type="text" style="display:none;" class="form-control" placeholder="Username" name="username">
  </div>
<form id="uploadForm" action="/students/profile" method="post" enctype="multipart/form-data" accept="image/jpeg, image/gif, image/x-png">
    Upload a Profile Picture:
    <input type="file" name="userFile" >
    <input type="submit" value="Upload Image" name="submit">
  </form>
    <form method="get" action="/createPortfolios">
     <button type="submit" class="btn btn-default">Do You Want To Create Portfolio?
       <span class="glyphicon glyphicon-book" aria-hidden="true"></span>
      </button>
    </form>
    <form method="get" action="/portfolios">
     <button type="submit" class="btn btn-default">Already Having A Portfolio?
       <span class="glyphicon glyphicon-ok" aria-hidden="true"></span>
      </button>
    </form>

</body>
